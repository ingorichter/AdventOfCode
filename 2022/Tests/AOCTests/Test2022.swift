//
//  Test2022.swift
//  AOCTests
//
//  Created by Ingo Richter on 11/13/22.
//  Copyright © 2022 Ingo Richter. All rights reserved.
//

import XCTest
@testable import AOC2022

final class Test2022: XCTestCase {
    func testDay1() async throws {
        let day = Day1()
        let (part1, part2) = try await day.run()

        XCTAssertEqual(part1, "70613")
        XCTAssertEqual(part2, "205805")
    }

    func testDay2() async throws {
        let day = Day2()
        let (part1, part2) = try await day.run()

        XCTAssertEqual(part1, "11386")
        XCTAssertEqual(part2, "13600")
    }

    func testDay3() async throws {
        let day = Day3()
        let (part1, part2) = try await day.run()

        XCTAssertEqual(part1, "7737")
        XCTAssertEqual(part2, "2697")
    }

    func testDay4() async throws {
        let day = Day4()
        let (part1, part2) = try await day.run()

        XCTAssertEqual(part1, "453")
        XCTAssertEqual(part2, "919")
    }

    func testDay5() async throws {
        let day = Day5()
        let (part1, part2) = try await day.run()

        XCTAssertEqual(part1, "MQSHJMWNH")
        XCTAssertEqual(part2, "LLWJRBHVZ")
    }

    func testDay6() async throws {
        let day = Day6()
        let (part1, part2) = try await day.run()

        XCTAssertEqual(part1, "1042")
        XCTAssertEqual(part2, "2980")
    }

    func testDay7() async throws {
        let day = Day7()
        let (part1, part2) = try await day.run()

        XCTAssertEqual(part1, "1491614")
        XCTAssertEqual(part2, "6400111")
    }

    func testDay8() async throws {
        let day = Day8()
        let (part1, part2) = try await day.run()

        XCTAssertEqual(part1, "1805")
        XCTAssertEqual(part2, "444528")
    }

    func testDay9() async throws {
        let day = Day9()
        let (part1, part2) = try await day.run()

        XCTAssertEqual(part1, "5695")
        XCTAssertEqual(part2, "2434")
    }

    func testDay10() async throws {
        let day = Day10()
        let (part1, part2) = try await day.run()

        XCTAssertEqual(part1, "")
        XCTAssertEqual(part2, "")
    }

    func testDay11() async throws {
        let day = Day11()
        let (part1, part2) = try await day.run()

        XCTAssertEqual(part1, "")
        XCTAssertEqual(part2, "")
    }

    func testDay12() async throws {
        let day = Day12()
        let (part1, part2) = try await day.run()

        XCTAssertEqual(part1, "")
        XCTAssertEqual(part2, "")
    }

    func testDay13() async throws {
        let day = Day13()
        let (part1, part2) = try await day.run()

        XCTAssertEqual(part1, "")
        XCTAssertEqual(part2, "")
    }

    func testDay14() async throws {
        let day = Day14()
        let (part1, part2) = try await day.run()

        XCTAssertEqual(part1, "")
        XCTAssertEqual(part2, "")
    }

    func testDay15() async throws {
        let day = Day15()
        let (part1, part2) = try await day.run()

        XCTAssertEqual(part1, "")
        XCTAssertEqual(part2, "")
    }

    func testDay16() async throws {
        let day = Day16()
        let (part1, part2) = try await day.run()

        XCTAssertEqual(part1, "")
        XCTAssertEqual(part2, "")
    }

    func testDay17() async throws {
        let day = Day17()
        let (part1, part2) = try await day.run()

        XCTAssertEqual(part1, "")
        XCTAssertEqual(part2, "")
    }

    func testDay18() async throws {
        let day = Day18()
        let (part1, part2) = try await day.run()

        XCTAssertEqual(part1, "")
        XCTAssertEqual(part2, "")
    }

    func testDay19() async throws {
        let day = Day19()
        let (part1, part2) = try await day.run()

        XCTAssertEqual(part1, "")
        XCTAssertEqual(part2, "")
    }

    func testDay20() async throws {
        let day = Day20()
        let (part1, part2) = try await day.run()

        XCTAssertEqual(part1, "")
        XCTAssertEqual(part2, "")
    }

    func testDay21() async throws {
        let day = Day21()
        let (part1, part2) = try await day.run()

        XCTAssertEqual(part1, "")
        XCTAssertEqual(part2, "")
    }

    func testDay22() async throws {
        let day = Day22()
        let (part1, part2) = try await day.run()

        XCTAssertEqual(part1, "")
        XCTAssertEqual(part2, "")
    }

    func testDay23() async throws {
        let day = Day23()
        let (part1, part2) = try await day.run()

        XCTAssertEqual(part1, "")
        XCTAssertEqual(part2, "")
    }

    func testDay24() async throws {
        let day = Day24()
        let (part1, part2) = try await day.run()

        XCTAssertEqual(part1, "")
        XCTAssertEqual(part2, "")
    }

    func testDay25() async throws {
        let day = Day25()
        let (part1, part2) = try await day.run()

        XCTAssertEqual(part1, "")
        XCTAssertEqual(part2, "")
    }

}
